git init 													начало работы
git add название.файла										добавить файл в коммит
git commit -m "название коммита"							надо не забывать называть коммиты
git commit --amend											сделать тот же коммит но с изменением
git remote add название.ветки хттп://название сайта			чтобы гит отслеживал эту ветку
git push 													запушить ветку на гитхаб
git push origin master										скопировать все новые коммиты в мастере на ориджин в удалённом репо
git push origin b1:b2										то же, через дробь источник : получатель
git checkout -b название.ветки								создать ветку и переключиться на неё
git checkout -b название.ветки origin/master				создать ветку, переключиться на неё и отслеживать её как удалённый master
git checkout название.ветки									просто переключись на другую ветку
git checkout название.коммита								переключить хеад на коммит
git checkout HEAD~сколько-то								перейти на сколько-то коммитов до хеад
git checkout HEAD^сколько-то								перейти на такого-то по порядку родителя мерджа
git merge имя.ветки											создать коммит из этой ветки + последних изменений другой ветки
git rebase имя.ветки										перенести финальную версию ветки в другую
git log														найти хеш нужного коммита
git checkout название.ветки^								предыдущий коммит в ветке
git checkout HEAD^											предыдущий коммит от текущего хеад
				~количество													вернуться на какое-то количество коммитов назад
git branch -f название.ветки HEAD~количество				переместить ветку в хеад (- сколько-то коммитов)
git reset HEAD~1											отменить 1 изменение и вернуть назад
git revert HEAD												
git cherry-pick коммит.1 коммит.2 коммит.н 					копировать коммиты в хеад
git rebase -i HEAD~количество								перейти в редактор для перестановки коммитов					
git tag название.тега название.коммита						присвоить тег какому-то коммиту как постоянную ссылку, если не коммиту, то сделает для хеад
git describe название.ветки									определить, как далеко тег, если без указания, от чего -- считает для хеад										
git bisect
git blame
git clone
git fetch													скачать данные с удалённого репо
git pull													это гит фетч + гит мердж
git pull --rebase											выпулить ветвь, сделать на неё ребэйс
git fakeTeamwork
git branch -u origin/master название.ветки					создать такую-то ветвь, сделав её отслеживающей удалённую master